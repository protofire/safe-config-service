apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rootstock-config-service-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: ingress.hostname
    http:
      paths:
      - backend:
          service:
            name: rootstock-config-service
            port:
              number: 443
        path: /
        pathType: Prefix
